# Environment Variables for StockPA.ai

# Google OAuth Configuration
# Get these from Google Cloud Console
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# JWT Configuration
# Generate secure random strings for production
JWT_SECRET=your-jwt-secret-key-here
JWT_REFRESH_SECRET=your-jwt-refresh-secret-here

# Database Configuration (auto-configured in docker-compose)
DATABASE_URL="postgresql://stockpa:stockpa123@localhost:8003/stockpa_db?schema=public"

# Redis Configuration (auto-configured in docker-compose)
REDIS_URL=redis://localhost:6379

# AI Engine Configuration (auto-configured in docker-compose)
OLLAMA_URL=http://localhost:8005

# Frontend Configuration (auto-configured in docker-compose)
FRONTEND_URL=http://localhost:8002

# Optional: External API Keys (for enhanced features)
NGX_API_KEY=your-ngx-data-api-key
YAHOO_FINANCE_API_KEY=your-yahoo-finance-api-key

# Optional: Email Configuration (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Optional: Payment Providers (for subscription features)
PAYSTACK_SECRET_KEY=sk_test_your-paystack-secret-key
FLUTTERWAVE_SECRET_KEY=FLWSECK_TEST-your-flutterwave-secret-key